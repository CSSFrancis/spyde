node_modules/
out/
dist/
test-results/
playwright-report/
*.log
# Generated by scripts/bundle-python.mjs (the staged Python sidecar payload —
# project source + uv.lock + the uv binary; regenerated on every `npm run dist`).
resources/python/
# Vendored uv binaries for packaging are large; CI populates them per-OS.
vendor/
