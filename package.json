{
  "name": "de-apps-root",
  "private": true,
  "description": "Monorepo root — shared shell in packages/, apps in apps/ (SpyDE still at electron/ + spyde/).",
  "workspaces": [
    "packages/*",
    "electron",
    "apps/*"
  ],
  "scripts": {
    "_comment:dev": "Bare `dev`/`build`/`test` mean SPYDE, for continuity with before the split. The other two apps are named explicitly rather than folded in — running the wrong app is a confusing failure, and `dev:groundcrew` cannot be mistaken for anything.",
    "dev": "npm --workspace spyde-electron run dev",
    "dev:groundcrew": "npm --workspace de-groundcrew run dev",
    "dev:autopilot": "npm --workspace de-autopilot run dev",
    "test:groundcrew": "npm --workspace de-groundcrew test",
    "test:autopilot": "npm --workspace de-autopilot test",
    "build": "npm --workspace spyde-electron run build",
    "preview": "npm --workspace spyde-electron run preview",
    "test": "npm --workspace spyde-electron test",
    "typecheck": "npm --workspace spyde-electron run typecheck",
    "_comment:test:unit": "--if-present SKIPS a workspace with no test:unit script, so this reports success when it has run NOTHING — which it did until shell-main's script was named to match. If you rename a package's test script, check the run still reports the expected test count.",
    "test:unit": "npm run --workspaces --if-present test:unit"
  }
}
